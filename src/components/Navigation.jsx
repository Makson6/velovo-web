export default function Navigation() {
   return (
      <nav className="gap-8 font-medium hidden md:flex">
         <a to="/">Acceuil</a>
         <a to="/"> Achat</a>
         <a to="/">Location</a>
      </nav>
   );
}